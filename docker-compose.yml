services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - 9090:9090

  # cortex:
  #   image: cortexproject/cortex:latest
  #   ports:
  #     - 9009:9009
  #   command: -config.file=/etc/cortex/cortex-config.yml
  #   volumes:
  #     - ./cortex-config.yml:/etc/cortex/cortex-config.yml

  # grafana:
  #   image: grafana/grafana:latest
  #   ports:
  #     - 3000:3000
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - ./grafana:/var/lib/grafana
